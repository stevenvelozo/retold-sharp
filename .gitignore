.DS_Store

# Logs
logs
*.log
npm-debug.log*

# Coverage
coverage
.nyc_output

# Dependencies (sharp installs into here at install time)
node_modules
node_modules/

# Lockfiles — retold-sharp pins via config.sharpVersion, not a lockfile
package-lock.json

# Output (root + nested under example_applications/*/)
dist
*.tgz
